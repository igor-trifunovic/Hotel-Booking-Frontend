# Base URL of the hotel-booking REST API.
# Copy this file to .env.local and adjust if your backend runs elsewhere.
# Falls back to http://localhost:8080 when unset.
REACT_APP_API_URL=http://localhost:8080
